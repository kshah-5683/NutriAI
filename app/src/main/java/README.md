# Kotlin & Java Source Roots (`app/src/main/java/`)

This directory is the root folder for the Kotlin/Java source code compiled into the Android application.

## 🎯 Major Function & Purpose

The `java` folder organizes source code modules by package namespaces. It acts as the container for the project's logic, database connections, dependency injection, and UI.

The application's core logic is located in the package directory:
* **[`com/app/nutriai/`](file:///C:/Users/Khushi%20Shah/Documents/GitHub/NutriAI/app/src/main/java/com/app/nutriai)**: Contains the Clean Architecture structure (`domain`, `data`, and `presentation` layers) for the Android client. Refer to the namespace README for details on the Kotlin code structure.
